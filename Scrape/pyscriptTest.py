#from bs4 import BeautifulSoup
#consider renaming this to original file names (and adjust the file names under previousattempts...)
import requests
def find(*args, **kwargs):
    product= Element('test-input').element.value
    Element('test-output').element.innerText = product
    

